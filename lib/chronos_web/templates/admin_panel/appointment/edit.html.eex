<h1>Edit Appointment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.admin_panel_appointment_path(@conn, :update, @appointment)) %>

<span><%= link "Back", to: Routes.admin_panel_appointment_path(@conn, :index) %></span>
